// Values that read flags root no selection rule — directly (setcc-shaped) or
// through a `let` shared with a flag output (adc-shaped). An instruction whose
// flag output conditionally preserves its old value is not a complete flag
// definition, so it never fuses as a definer.

// RUN: tmdlc --action=emit-rust --dialect=test --output=- %S/../Inputs/flag-value-rules.tmdl | filecheck %s

// CHECK-NOT: fn isel_pattern_setcclike(
// CHECK-NOT: fn isel_pattern_adclike(
// CHECK-NOT: via_cmppreserve
