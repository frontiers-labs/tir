// RUN: not tir mc --march=rv64i --stage=isel %s

// i128 is wider than the selected XLEN and is rejected before selection.

module {
  %fn_a = func.func @a(%0: !i128, %1: !i128) -> !i128 {
    %2 = addi %0, %1 : !i128
    func.return %2
  }
  module_end
}
