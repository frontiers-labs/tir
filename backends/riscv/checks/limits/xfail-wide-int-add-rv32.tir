// RUN: not tir mc --march=rv32i --stage=isel %s

// i64 is wider than rv32 XLEN and is rejected before selection.

module {
  %fn_a = func.func @a(%0: !i64, %1: !i64) -> !i64 {
    %2 = addi %0, %1 : !i64
    func.return %2
  }
  module_end
}
