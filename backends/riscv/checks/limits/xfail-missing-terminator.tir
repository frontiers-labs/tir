// RUN: not tir mc --march=rv64i --stage=isel %s

// The mc pipeline verifies TIR before lowering and rejects missing terminators.

module {
  %fn_a = func.func @a(%0: !i64) -> !i64 {
    %1 = addi %0, %0 : !i64
  }
  module_end
}
