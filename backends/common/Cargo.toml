[package]
name = "tir-be-common"
version = "0.1.0"
edition = "2024"

[dependencies]
tir = { path = "../../core" }
logos = "0.15.0"

[dev-dependencies]
insta = "1.43.1"
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "lexer"
harness = false
