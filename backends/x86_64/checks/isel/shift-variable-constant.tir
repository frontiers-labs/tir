// RUN: tir mc --march=x86_64 --stage=regalloc %s | filecheck %s

module {
  func.func @shift_one(%count: !i64) -> !i64 {
    %one = constant {value = 1} : !i64
    %result = shli %one, %count : !i64
    func.return %result
  }
  module_end
}

// CHECK: x86_64.mov_imm
// CHECK: x86_64.shl_cl
