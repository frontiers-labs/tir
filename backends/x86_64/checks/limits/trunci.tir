// RUN: tir mc --march=x86_64 --stage=isel %s | filecheck %s

// i64 -> i32 truncation selects a 32-bit move, preserving the low bits.

module {
  func @tr(%0: !i64) -> !i32 {
    %1 = trunci %0 : !i32
    return %1
  }
  module_end
}

// CHECK: x86_64.mov32
