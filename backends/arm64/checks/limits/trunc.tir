// RUN: tir mc --march=arm64 --stage=isel %s | filecheck %s

// i64 -> i32 truncation selects UBFM extracting bits [31:0].

module {
  func @f(%0: !i64) -> !i32 {
    %1 = trunci %0 : !i32
    return %1
  }
  module_end
}

// CHECK: arm64.ubfm
// CHECK-SAME: immr = 0
// CHECK-SAME: imms = 31
