// RUN: tir mc --march=arm64 --stage=isel %s | filecheck %s

module {
  %fn_copy = func.func @copy(%0: !ptr.p, %1: !ptr.p, %2: !i64) {
    ptr.memcpy %0, %1, %2
    func.return
  }
  module_end
}

// CHECK: asm.vcall | %{{[0-9]+}} {callee = "memcpy"
// CHECK-NOT: ptr.memcpy
