name: fcc package
description: Restore the fcc and xtask binaries built by the `package` job.
runs:
  using: composite
  steps:
    - uses: actions/download-artifact@v8
      with:
        name: fcc-package
        path: target
    - shell: bash
      run: chmod +x target/release/fcc target/debug/xtask
