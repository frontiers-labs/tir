// A constant with a dynamic width guards the APInt width range where the width
// is computed, which is the host function the head reads it from.
// RUN: tir-pdl %s --emit rust | filecheck %s

rule zero: builtin.subi(x: int<W>, x) => const<W>(0);

// CHECK: if !(1..=64).contains(&(width as u32))
