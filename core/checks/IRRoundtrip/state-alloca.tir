// RUN: tir opt %s | filecheck %s

module {
  func.func @f() {
    %0 = ptr.alloca {size = 4, align = 4} : !ptr.p<!i32> state(-> %1)
    func.return
  }
  module_end
}

// CHECK: %{{[0-9]+}} = ptr.alloca {size = 4, align = 4} : !ptr.p<!i32> state(-> %{{[0-9]+}})
// CHECK-NEXT: func.return
