// RUN: tir opt %s | filecheck %s

module {
  %fn_f = func.func @f() {
    %0, state(%1) = ptr.alloca {size = 4, align = 4} : !ptr.p<!i32>
    func.return
  }
  module_end
}

// CHECK: %{{[0-9]+}}, state(%{{[0-9]+}}) = ptr.alloca {size = 4, align = 4} : !ptr.p<!i32>
// CHECK-NEXT: func.return
