// RUN: tir opt %s | filecheck %s

module {
  %fn_f = func.func @f() {
    state(%0) = state.entry_state
    func.return
  }
  module_end
}

// CHECK: %{{[0-9]+}} = func.func @f() {
// CHECK-NEXT: state(%{{[0-9]+}}) = state.entry_state
// CHECK-NEXT: func.return
