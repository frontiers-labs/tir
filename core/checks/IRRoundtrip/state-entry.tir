// RUN: tir opt %s | filecheck %s

module {
  func @f() {
    %0 = state.entry_state : !state
    return
  }
  module_end
}

// CHECK: func @f() {
// CHECK-NEXT: %{{[0-9]+}} = state.entry_state : !state
// CHECK-NEXT: return
