// RUN: tir opt %s | filecheck %s

module {
  %fn_loop = func.func @loop(%0: !i1) {
    scf.while {
      scf.condition %0
    } do scope(%1) {
      scf.break %1
    }
    func.return
  }
  module_end
}

// CHECK: scf.while {
// CHECK: do scope(%[[SCOPE:[0-9]+]]) {
// CHECK-NEXT: scf.break %[[SCOPE]]
