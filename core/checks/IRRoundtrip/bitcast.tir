// RUN: tir opt %s | filecheck %s

module {
  %fn_to_double = func.func @to_double(%0: !i64) -> !f64 {
    %1 = bitcast %0 : !f64
    func.return %1
  }
  %fn_to_integer = func.func @to_integer(%0: !f64) -> !i64 {
    %1 = bitcast %0 : !i64
    func.return %1
  }
  module_end
}

// CHECK: %{{[0-9]+}} = func.func @to_double(%0: !i64) -> !f64 {
// CHECK-NEXT: %1 = bitcast %0 : !f64
// CHECK-NEXT: func.return %1
// CHECK: %{{[0-9]+}} = func.func @to_integer(%{{[0-9]+}}: !f64) -> !i64 {
// CHECK-NEXT: %{{[0-9]+}} = bitcast %{{[0-9]+}} : !i64
// CHECK-NEXT: func.return %{{[0-9]+}}
