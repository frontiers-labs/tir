// RUN: tir opt %s | filecheck %s

module {
  %fn_copy = func.func @copy(%0: !ptr.p, %1: !ptr.p, %2: !i64) {
    ptr.memcpy %0, %1, %2
    func.return
  }
  module_end
}

// CHECK: module {
// CHECK-NEXT: %{{[0-9]+}} = func.func @copy(%{{[0-9]+}}: !ptr.p, %{{[0-9]+}}: !ptr.p, %{{[0-9]+}}: !i64) {
// CHECK-NEXT: ptr.memcpy %{{[0-9]+}}, %{{[0-9]+}}, %{{[0-9]+}}
// CHECK-NEXT: func.return
// CHECK-NEXT: }
// CHECK-NEXT: module_end
// CHECK-NEXT: }
