// RUN: tir opt --verify %s | filecheck %s

module {
  %fn_axpy = func.func @axpy(%0: !ptr.p, %1: !ptr.p, %2: !i32) noalias [0, 1] {
    func.return
  }
  module_end
}

// CHECK: %{{[0-9]+}} = func.func @axpy(
// CHECK-SAME: noalias [0, 1] {
