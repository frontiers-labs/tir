// RUN: not tir opt --verify %s

// A declaration names a definition supplied elsewhere, so it can never be
// private to this module.

module {
  %fn_external = func.declare private @external(!i32) -> !i32
  module_end
}
