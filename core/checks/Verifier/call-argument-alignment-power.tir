// RUN: not tir opt --verify %s

module {
  %fn_consume = func.declare @consume(!i64) -> !unit
  %fn_bad = func.func @bad(%0: !i64) {
    func.call %fn_consume(%0 : !i64) argument_alignments [3]
    func.return
  }
  module_end
}
