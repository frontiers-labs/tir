// RUN: not tir opt --verify %s 2>&1 | filecheck %s

// An unordered body produces exactly what its function declares.

module {
  %fn_f = func.func @f(%0: !i32) -> !i32 {
    %1 = addi %0, %0 : !i32
    -> %1, %1
  }
  module_end
}

// CHECK: produces (!i32, !i32), but its signature returns !i32
