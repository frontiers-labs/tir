// RUN: not tir opt --verify %s

// A bitcast preserves the complete bit pattern, so its source and result must
// have the same width.

module {
  func @bad(%0: !i32) -> !f64 {
    %1 = bitcast %0 : !f64
    return %1
  }
  module_end
}
