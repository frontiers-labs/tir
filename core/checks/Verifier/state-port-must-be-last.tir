// RUN: not tir opt --verify %s

module {
  %fn_bad = func.func @bad(%0: !ptr.p) {
    %1 = state.entry_state : !state
    %2 = func.indirect_call %0(%1, %0 : !state, !ptr.p) -> !i32
    func.return
  }
  module_end
}
