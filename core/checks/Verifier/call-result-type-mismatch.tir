// RUN: not tir opt --verify %s

// The call result type must match the return type of the resolved overload.

module {
  declare @f(!i32) -> !i32
  func @caller(%0: !i32) -> !i64 {
    %1 = call @f(%0 : !i32) -> !i64
    return %1
  }
  module_end
}
