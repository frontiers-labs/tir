// RUN: not tir opt --verify %s

// addf requires float operands; integer operands fail verification even when
// the annotated result type is a float.

module {
  %fn_f = func.func @f() -> !f32 {
    %0 = constant {value = 1} : !i32
    %1 = constant {value = 2} : !i32
    %2 = addf %0, %1 : !f32
    func.return %2
  }
  module_end
}
