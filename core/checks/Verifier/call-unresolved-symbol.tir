// RUN: not tir opt --verify %s

// A call must name a function the module's symbol table knows about.

module {
  func.func @caller(%0: !i32) -> !i32 {
    %1 = func.call @missing(%0 : !i32) -> !i32
    func.return %1
  }
  module_end
}
